# Web production env (Vercel project settings). The web app is a server-side BFF, so this is read
# by the Next.js SERVER (not the browser). Point it at the deployed Render API.

# Base URL of the SahaHR API on Render (no trailing slash):
SAHAHR_API_URL=https://sahahr-api.onrender.com
