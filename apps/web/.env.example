# Copy to .env.local (gitignored). Tells the Next.js SERVER process where the API is.
# Required if the API is not on the default below — otherwise the login server action
# can't reach /v1/dev/token and the page silently stays on /login.
SAHAHR_API_URL=http://127.0.0.1:5080
